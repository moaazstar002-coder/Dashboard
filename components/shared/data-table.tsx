import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function DataTable() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Data Table</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Data table component template</p>
      </CardContent>
    </Card>
  );
}
